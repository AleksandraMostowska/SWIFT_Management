package mostowska.aleksandra.model.dto;

/**
 * GetModelDto is an interface that serves as a marker for DTOs that are used
 * to represent models in the system. It is a general type for DTOs that can
 * be used to transfer data between different layers of the application.
 *
 * DTOs implementing this interface are expected to encapsulate the necessary data
 * related to different entities, such as a bank's branch or headquarters, and
 * provide a standardized structure for data representation.
 */
public interface GetModelDto {
}
